<% breadcrumb :deliveries, @board %>

<h3>Deliveries</h3>

<% @projects.each do |project| %>
    <h5>
      <a href="<%= "#{reports_path(@board)}/deliveries/#{project.key}" %>">
        <%= project.key %>
      </a>
      &mdash;
      <%= project.summary %>
    </h5>
<% end %>
